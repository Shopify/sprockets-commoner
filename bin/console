#!/usr/bin/env ruby

require 'bundler/setup'
require 'sprockets'
require 'sprockets/commoner'

require 'pry'

env = Sprockets::Environment.new(File.expand_path('../tmp/env', __dir__))
env.append_path File.expand_path('../tmp/env/assets', __dir__)
$stderr.puts "'env' has been set up"

binding.pry
